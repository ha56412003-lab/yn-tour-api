# 2026-04-17 Daily Notes

## Session: 上午（Bug修复）

### 4个Bug修复完成
1. 数据看板字段错位 → `data.orderStats?.totalOrders` 等正确前缀
2. 营收日期筛选无效 → 后端 now respects startDate/endDate
3. 佣金冻结无解冻 → 新增 checkAndReleaseFrozenCommissions，30天自动解冻
4. 提现超提漏洞 → 新增 availableBalance 校验

---

## Session: 下午（279规则适配）

### 浩哥发送279说明书（云南团游279分销机制说明书·用户版）
对照文档发现后端实现缺失多个模块，全部修复完成。

### 279文档核心定义（对照后端实现）

**「2」二级分润**
| 层级 | 规则 |
|------|------|
| 直推（一级） | 第1单30%/第2单70%/循环 |
| 间推（二级） | 固定10% = ¥79.9 |

**「7」七人成团**
- 7人成团，800元/团，循环出局
- 退款订单不算点位（从订单创建时就判定）

**「9」永久分红（三种叠加）**
1. 平台业绩分红：当月2%营收，按订单占比
2. 成团专项分红：每成1个7人团，额外该团总交易额×2%（≈¥111.86/团）
3. 直推管理津贴：直推用户所有收益×10%（月度结算）

### 有效分享奖（0.5元/次）
**已取消**，不遵循文档

### 实现变更清单

**Order模型**：新增 `grandParentId`、`isValid(true默认)`、`indirectProfit`、`groupDividend`

**下单时**：自动写入 `grandParentId`（找推荐人的推荐人）

**间推分润（10%）**：
- C下单付款 → 查 grandParentId（A）→ 创建 type:'indirect' 佣金记录，冻结
- 金额：799×10% = ¥79.9

**成团奖+成团专项分红**：
- 成团奖：800元/团
- 成团专项分红：5593×2% ≈ ¥111.86/团（每成团立即发放）
- 仅统计 `isValid=true` 订单

**直推管理津贴（加入月度分红）**：
- 直推用户当月所有佣金（直推+成团+间推）×10%
- 每月5日结算

**279循环计数修复**：
- 移除 lockedUserOrderNum，纯用 directPushNum 计算奇偶

**退款逻辑**：
- 审核通过后 isValid=false，订单不再计入7人团点位

### 279实现状态（✅全部完成）
- 直推分润（30%/70%循环）✅
- 间推分润（10%）✅
- 成团奖（800元/团）✅
- 成团专项分红（该团×2%）✅
- 平台业绩分红（月度2%）✅
- 直推管理津贴（直推收益×10%）✅
- 佣金30天冻结+自动解冻 ✅
- 退款佣金追回 ✅
- 退款订单不算点位 ✅

---

## 技术备注

### 前端文案澄清
文档里"二级分销商拿70%"指的是第2单70%循环奖励（奇偶循环），不是二级分销商得70%。真正的二级（间推）得10%固定。**前端无Bug，无需修改**，只是文案有歧义。

### 后端锁客机制（lockedUserOrderNum）
这是之前开发遗留的额外机制。279文档中无此概念，但作为公司额外激励保留。对279规则的影响：
- 成团奖/分红：lockedUserOrderNum 仍然计入团队总单量（算作有效下级）
- 279循环计数：**已排除**（只算 directPushNum）

### Server状态
运行在端口3049，日志正常

---

## 下一步（待浩哥决策）
- 279规则已完整实现，待测试验证
- 如有新需求或测试反馈，继续迭代

---

## Session: 下午（排行旁时间筛选 + 两个待办确认 + 管家头像）

### 排行榜时间筛选修复 ✅
- 后端 `getTeamRanking` 支持 `period` 参数：`month` / `lastMonth` / `all`
- 前端 `ranking.vue` 切换 Tab 时调用 `loadRanking()` 重新加载
- 当月/上月：按佣金记录时间聚合；累计：直接用 totalEarnings

### 剩余两个待办（浩哥确认方案）

**1. 退款API对接（待办1）**
- 现状：管理后台点"审核通过"只改状态，不真实退款
- 方案：接微信支付退款API + 支付宝退款API
- 浩哥回复：等对公账户开好后处理

**2. 提现打款自动化（待办2）**
- 现状：审核通过 → 待打款 → 需再点一次"处理提现"
- 方案B：管理员审核通过 → 进入打款队列 → 系统自动打款（微信支付商家转账/支付宝商家转账）
- 浩哥回复：按方案B处理

### 专属管家头像
- user.vue 已引用 `/static/butler-avatar.png`
- 头像原文件不存在，下载了专业女性旅行管家图保存为 `butler-avatar.png`
- 文件路径：`yn-tour-distribution/static/butler-avatar.png`

---

## 技术备注
- Server端口3049，运行正常
- 构建每次都成功：`npm run build:h5`
- 新增图片时注意：下载的是JPEG格式，需用 `mv` + `sips -s format png` 转换扩展名


---

## Session: 下午（CSV数据验证 + 管家头像）

### 浩哥发送团队测试数据CSV
路径：`/Users/tiaotiao/.openclaw/media/inbound/表格_20260417---e53759b0-ebb0-4a41-bb2e-82804be12922.csv`

数据：10人团队（A是团长，推荐关系A→B/C/D, B→E/F/J, C→G, D→H, E→I, F→J）

**发现数据内部不一致：**
- `直推单` ≠ 直接下级人数（如C直接下级=G（1人），但直推单=5）
- `团队总单量` ≠ 自购单+直推单之和（如A：1+9=10，但团队=23）
- 说明：直推单可能是直推订单数（非人数），团队总单量可能是累计订单金额或其他计算方式

**已向浩哥确认意图：** 等待浩哥回复这个数据是用于核实279佣金计算还是其他目的
---

## Session: 下午（海报背景管理 + 排行榜问题排查）

### 海报背景管理 ✅
- admin-web 新增「海报背景」菜单（挂载在「首页海报配置」下）
- 功能：上传/启用禁用/排序/删除背景图
- 已有3张背景图已录入
- 后端：新增 `GET/POST /api/admin/backgrounds` 和 `DELETE /api/admin/backgrounds/:id`
- 海报生成时从已启用的背景中随机选一张

### 排行榜数据未更新（进行中）
- 浩哥反馈：小程序首页第3屏排行榜仍显示旧测试账号，10个新虚拟账户（A-J）没上去
- 排查方向：
  1. 排行榜排序字段可能是 `totalEarnings`（佣金），但A-J用户 `totalEarnings` 都是0
  2. 或是 `totalTeamOrder`（团队订单数），但前端可能没有暴露这个字段
  3. 排行榜页面在 `pages/ranking/ranking.vue`，需确认它调用哪个API、按什么字段排序
  4. 数据库中 A-J 的 `totalTeamOrder` 值（A:10, B:8, C:6, D:5, E:4, F:7, G:3, H:2, I:9, J:1）
- 待查：小程序排行榜前端调用API和数据结构
