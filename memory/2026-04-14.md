# 2026-04-14 工作日志

## 抖店小程序后端 + 海报生成修复

### 项目信息
- 路径: `/Users/tiaotiao/.openclaw/workspace-fengxi/yn-tour-miniprogram/server`
- 后端端口: 3000
- MongoDB: localhost:27017 (database: **yn-tour**)
- 启动脚本: `bash /tmp/start_yn_tour_server.sh`

### 已完成
1. **后端框架搭建**: 登录/商品/地址/订单/提现/管理 API
2. **商品数据初始化**: 云南旅游路线 6天5晚 ¥2760
3. **海报生成后端接口**: `GET /api/user/poster-image?userId=xxx`
   - 返回: `{ code: 200, data: { posterUrl: "/uploads/posters/poster_xxx.png" } }`
   - 海报尺寸: 540×886 px（绿色背景 + 装饰圆圈 + 右下角二维码）
   - 4张随机背景图: `uploads/backgrounds/` (yn_bg.jpg, yn_bg2.jpg, yn_bg3.jpg)

### Jimp 1.x API 注意事项
- 导入: `const { Jimp } = require('jimp')`
- 创建图片: `new Jimp({ width, height, color })`
- 缩放: `image.resize({ w, h })`
- 写入: `poster.write(filepath)` (同步)
- ⚠️ 注意：`circle()` 和 `radius()` 不是有效方法

### 前端修改
- share.vue: `generatePoster()` 改用调用后端 `/api/user/poster-image` 接口
- 关闭按钮: 固定左上角 (`position: fixed; top: 20rpx; left: 20rpx`)
- 海报显示: `width: 100%` + `mode="aspectFit"`
- join.vue: 同样关闭按钮改左上角

### 测试账号
- userId: `69de2e4beff31649d8b3149f`
- openid: `test_fengxi_001`
- isDistributor: true

### 待处理（明天继续）
- 海报尺寸显示效果优化（手机端偏小的问题未完全解决）
- `webapi_getwxaasyncsecinfo` 警告（微信开发者工具游客模式限制，换真实账号可消除）

### MongoDB 连接串
- `mongodb://localhost:27017/yn-tour?retryWrites=true&w=majority`
